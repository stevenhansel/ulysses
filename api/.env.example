# Database connection string (SQLite)
DATABASE_URL=sqlite:./data/ulysses.db?mode=rwc

# Server host and port
HOST=0.0.0.0
PORT=8000

# Logging level
# Options: trace, debug, info, warn, error
RUST_LOG=ulysses_api=debug,tower_http=debug
